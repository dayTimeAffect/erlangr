/**
 * @Description:
 * @author dayTimeAffect
 * @date 2020/8/28
 */
import baseArrayMove from "../.internal/baseArrarMove.js";

const arrayMove = (array, start, end) => {
    return baseArrayMove(array, start, end)
}

export default arrayMove